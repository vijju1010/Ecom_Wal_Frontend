import React from 'react';
import { Link } from 'react-router-dom';
const Profile = () => {
    return (
        <div>
            <Link to='/placedorders'>Placed Orders</Link>
        </div>
    );
};

export default Profile;
